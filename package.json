{
  "name": "wacko-portfolio",
  "version": "2.3.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node start.mjs",
    "check": "node --check start.mjs && node --check server.mjs && node --check src/background.js && node --check src/assets/particles.min.js && node --test test/*.test.mjs",
    "build": "node build.mjs"
  }
}
